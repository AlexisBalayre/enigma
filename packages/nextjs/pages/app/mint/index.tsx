import EncryptDecryptComponent from "~~/components/EncryptDecryptWidget";
import { AppNavWrapper } from "~~/components/app/Nav";

export default function Mint() {
  return (
    <AppNavWrapper>
      <div className="text-white">hi, the minting should go here</div>
      <EncryptDecryptComponent />
    </AppNavWrapper>
  );
}
